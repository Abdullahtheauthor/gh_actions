on:
  release:



jobs:
  Tagging:

    if : startsWith(github.ref, 'refs/tags/v.') && !endsWith(github.ref, '*preprod.')
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v3
